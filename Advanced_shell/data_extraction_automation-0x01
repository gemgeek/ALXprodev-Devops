#!/bin/bash
jq -r '.name, .types[0].type.name, .weight, .height' data.json | xargs | awk '{name=toupper(substr($1,1,1)) substr($1,2); type=toupper(substr($2,1,1)) substr($2,2); weight=$3/10; height=$4/10; printf "%s is of type %s, weighs %skg, and is %sm tall.\n", name, type, weight, height}'